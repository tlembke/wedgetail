<%=render :partial=>"layouts/careplanmenu"%>
<h1>Listing measures</h1>
Measure are numerical results that can be attached to goals.<p>

<table border=1>
  <tr>
    <th>Name</th>
    <th>Abbreviation</th>
     <th>Description</th>

    <th>Target</th>
 

  </tr>

<% @measures.each do |measure| %>
  <tr>
    <td><%=h measure.name %></td>
    <td><%=h measure.abbreviation %></td>
		<td><%=h measure.description %></td>
    <td><%=h measure.operator_string %><%=number_with_precision(measure.value,measure.places)+" " if measure.value%><%=h measure.units %></td>

    <td><%= link_to 'Show', measure %></td>
    <td><%= link_to 'Edit', edit_measure_path(measure) %></td>
    <td><%= link_to 'Destroy', measure, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New measure', new_measure_path %>